# 外部身份连接器元数据声明、动态发现与运行时加载链路分析

## 1. 元数据声明规范与约束

### 1.1 元数据类型体系

外部身份连接器的元数据定义位于 `packages/toolkit/connector-kit/src/types/metadata.ts`，采用分层设计：

#### 1.1.1 基础连接器元数据 (`ConnectorMetadata`)

```typescript
type ConnectorMetadata = {
  id: string;                    // 连接器工厂唯一标识
  target: string;                // 身份提供商标识，同一provider仅允许一个身份
  name: I18nPhrases;             // 多语言名称，必须包含英文
  description: I18nPhrases;      // 多语言描述
  logo: string;                  // Logo 图片路径或URL
  logoDark: Nullable<string>;    // 暗色模式Logo
  readme: string;                // 说明文档路径
  configTemplate?: string;       // 配置模板
  formItems?: ConfigFormItem[];  // 配置表单项定义
  customData?: Record<string, unknown>;
} & SocialConnectorMetadata;
```

**核心约束**：
- `id`：全局唯一的连接器工厂ID，用于加载时匹配
- `target`：身份提供商标识，同一用户在同一target下只能有一个社交身份。SSO连接器中与issuer一致
- `name` / `description`：必须包含英文翻译，其他语言可选，通过 `i18nPhrasesGuard` 验证

#### 1.1.2 社交连接器扩展元数据 (`SocialConnectorMetadata`)

```typescript
type SocialConnectorMetadata = {
  platform: Nullable<ConnectorPlatform>;  // Native | Universal | Web
  isStandard?: boolean;                   // 是否遵循标准协议(OIDC/OAuth2/SAML2)
  isTokenStorageSupported?: boolean;      // 是否支持Token持久化存储
};
```

#### 1.1.3 SSO 连接器元数据 (`SingleSignOnFactory`)

SSO连接器采用独立的元数据体系，定义于 `packages/core/src/sso/types/index.ts`：

```typescript
type SingleSignOnFactory<T extends SsoProviderName> = {
  providerName: T;               // 内部提供商名称枚举
  providerType: SsoProviderType; // 提供商类型 (OIDC / SAML)
  logo: string;
  logoDark: string;
  description: I18nPhrases;
  name: I18nPhrases;             // 控制台展示用名称
  configGuard: ZodType;          // 配置验证Schema
  constructor: SsoConnectorConstructor;
};
```

### 1.2 元数据验证机制

#### 1.2.1 `connectorMetadataGuard` 职责与触发位置

**职责**：
- 定义完整的连接器元数据 Zod Schema
- 验证所有必填字段的存在性和格式正确性
- 合并社交连接器扩展字段（platform, isStandard, isTokenStorageSupported）
- 不使用 `.catchall()`，严格禁止未知字段（v4.0.0 后移除了 catchall）

**触发位置**：

| 调用方 | 文件路径 | 用途 |
|-------|---------|------|
| `connectorResponseGuard` | `packages/schemas/src/types/connector.ts:18` | API 响应结构验证，`.merge(connectorMetadataGuard)` |
| `connectorFactoryResponseGuard` | `packages/schemas/src/types/connector.ts:35` | 工厂列表响应验证 |
| `fullSignInExperienceGuard` | `packages/schemas/src/types/sign-in-experience.ts:60` | 登录体验配置中的社交连接器列表 |

#### 1.2.2 `validateConnectorModule` 职责与触发位置

**职责**：
- TypeScript 类型断言函数 (`asserts connector is BaseConnector`)
- 运行时最小完整性检查（非 Schema 级验证）
- 仅检查三个关键字段的存在性：`metadata`、`configGuard`、`type`

```typescript
export function validateConnectorModule(
  connector: Partial<BaseConnector<ConnectorType>>
): asserts connector is BaseConnector<ConnectorType> {
  if (!connector.metadata) throw new ConnectorError(ConnectorErrorCodes.InvalidMetadata);
  if (!connector.configGuard) throw new ConnectorError(ConnectorErrorCodes.InvalidConfigGuard);
  if (!connector.type || !Object.values(ConnectorType).includes(connector.type))
    throw new ConnectorError(ConnectorErrorCodes.UnexpectedType);
}
```

**触发位置**：

| 位置 | 时机 | 失败处理 |
|-----|------|---------|
| `packages/cli/src/connector/factories.ts:28` | 启动期工厂加载 | 记录错误日志 → skip 该连接器 → 服务继续启动 |
| `packages/cli/src/connector/utils.ts:112` | 运行时 `buildRawConnector` | 抛出异常 → 被上层空 catch 静默过滤 |

#### 1.2.3 验证 Guard 汇总

| Guard | 位置 | 验证内容 |
|-------|------|---------|
| `connectorMetadataGuard` | `metadata.ts:76` | 完整元数据Schema验证（严格模式，拒绝未知字段） |
| `i18nPhrasesGuard` | `metadata.ts:16` | 多语言字段必须包含 `en` 且键为合法语言标签 |
| `socialConnectorMetadataGuard` | `metadata.ts:53` | 社交连接器扩展字段验证 |
| `configurableConnectorMetadataGuard` | `metadata.ts:98` | 数据库可配置字段白名单 (target, name, logo, logoDark) |
| `validateConnectorModule` | `cli/src/connector/utils.ts:22` | 运行时最小完整性检查（metadata/configGuard/type） |

### 1.3 元数据解析流程

元数据解析在 `packages/cli/src/connector/utils.ts:82-94` 的 `parseMetadata` 函数中完成：

```
连接器声明的相对路径 → 读取本地文件 → 转换为Data URI (图片) 或 文本内容
```

- **Logo处理**：支持 `.svg`, `.png` 格式，转换为 `data:image/xxx;base64` 内联
- **Readme/ConfigTemplate**：读取为纯文本内容
- **HTTP资源**：以 `http` 开头的URL直接返回，不做本地读取

---

## 2. 启动期加载顺序与依赖解析

### 2.1 启动初始化流程

加载入口位于 `packages/core/src/main.ts:29-35`，与其他核心模块并行初始化：

```typescript
await Promise.all([
  initI18n(),
  redisCache.connect(),
  loadConnectorFactories(),    // 连接器工厂加载
  checkPreconditions(sharedAdminPool),
  SystemContext.shared.loadProviderConfigs(sharedAdminPool),
]);
```

### 2.2 普通连接器 (Social/Email/SMS) 加载链路

#### 2.2.1 动态发现包名准入规则

`packages/cli/src/utils.ts:239-272` 定义了连接器包的扫描和准入规则：

```typescript
const getConnectorPackageName = async (directory: string) => {
  const filePath = path.join(directory, 'package.json');
  if (!existsSync(filePath)) return;
  
  const { name } = z.object({ name: z.string() }).parse(JSON.parse(await readFile(filePath, 'utf8')));
  
  // 两种命名模式都被允许：
  // 1. 无 scope: name.startsWith('connector-')
  // 2. 有 scope: name.split('/')[1]?.startsWith('connector-')
  if (name.startsWith('connector-') || Boolean(name.split('/')[1]?.startsWith('connector-'))) {
    return name;
  }
};
```

**准入规则总结**：

| 包名格式 | 是否允许 | 示例 |
|---------|---------|------|
| `connector-xxx` | ✅ 允许 | `connector-google`, `connector-github` |
| `@scope/connector-xxx` | ✅ 允许 | `@logto/connector-kit`, `@acme/connector-oidc` |
| `xxx-connector` | ❌ 拒绝 | `google-connector` |
| `@scope/xxx-connector` | ❌ 拒绝 | `@acme/google-connector` |
| 无 package.json | ❌ 跳过 | - |

#### 2.2.2 加载流程图

```
packages/core/src/utils/connectors/index.ts
  └── loadConnectorFactories()
        ├── 查找 connectors 目录
        ├── getConnectorPackagesFromDirectory() 扫描目录
        │     └── getConnectorPackageName() 按命名规则过滤
        └── _loadConnectorFactories() [packages/cli/src/connector/factories.ts]
              ├── 检查缓存 cachedConnectorFactories
              └── 并行处理每个连接器包:
                    ├── loadConnector() [loader.ts] 版本检查 + 模块导入
                    ├── createConnector() 实例化工厂
                    ├── validateConnectorModule() 验证模块完整性
                    ├── parseMetadata() 解析元数据资源
                    └── 去重检查 checkDuplicateConnectorFactoriesId
```

#### 2.2.3 依赖版本约束

`packages/cli/src/connector/loader.ts:14-36` 实现了严格的 `@logto/connector-kit` 版本检查：

```typescript
const checkConnectorKitVersion = (dependencies: unknown, ignoreVersionMismatch: boolean) => {
  // 1. 必须在依赖中声明 @logto/connector-kit
  // 2. 版本必须满足 semver.satisfies(currentVersion, requiredVersion)
  // 3. workspace: 前缀在开发环境被豁免
  // 4. 可通过 ignoreVersionMismatch 降级为警告 (不推荐生产环境)
};
```

#### 2.2.4 模块加载兼容性

支持 CJS 和 ESM 两种模块格式：
- **ESM模式**：`import(path).default` 为构造函数
- **CJS模式**：`import(path).default.default` 为构造函数 (双包装)

#### 2.2.5 去重与唯一性约束

`packages/core/src/utils/connectors/index.ts:71-87` 中 `checkDuplicateConnectorFactoriesId`：
- 扫描所有连接器工厂的 `metadata.id`
- 发现重复ID时抛出 `connector.more_than_one_connector_factory` 错误
- 状态码 422，附带重复ID列表

### 2.3 SSO 连接器加载机制

SSO连接器采用**静态注册**而非动态扫描，定义于 `packages/core/src/sso/index.ts`：

```typescript
export const ssoConnectorFactories: {
  [key in SsoProviderName]: SingleSignOnFactory<key>;
} = {
  [SsoProviderName.OIDC]: oidcSsoConnectorFactory,
  [SsoProviderName.SAML]: samlSsoConnectorFactory,
  [SsoProviderName.AZURE_AD]: azureAdSsoConnectorFactory,
  [SsoProviderName.AZURE_AD_OIDC]: azureOidcSsoConnectorFactory,
  [SsoProviderName.GOOGLE_WORKSPACE]: googleWorkSpaceSsoConnectorFactory,
  [SsoProviderName.OKTA]: oktaSsoConnectorFactory,
};
```

**设计差异**：
- 普通连接器：从文件系统动态扫描加载，支持第三方扩展
- SSO连接器：编译期静态注册，强调安全性和稳定性

### 2.4 运行时连接器实例化 (`getLogtoConnectors`)

`packages/core/src/libraries/connector.ts:73-117` 实现从数据库配置到运行时实例的转换：

```
1. findAllConnectors() 从DB读取所有连接器配置
2. loadConnectorFactories() 获取已加载的工厂
3. 按 connectorId 匹配工厂和DB配置
4. 工厂不存在 → 跳过 (静默失败)
5. 工厂存在 → buildRawConnector() 实例化
6. 合并元数据 (rawMetadata ∪ dbMetadata)
7. 注入 validateConfig 方法
8. 绑定 dbEntry 供后续操作使用
```

关键代码：
```typescript
const connector: AllConnector = {
  ...defaultConnectorMethods,
  ...rawConnector,
  metadata: { ...rawMetadata, ...metadata },  // DB配置优先级更高
};
```

---

## 3. 运行期配置变更通知机制

### 3.1 Connectors-Well-Known 缓存失效路径

#### 3.1.1 缓存机制设计

`packages/core/src/caches/well-known.ts` + `packages/core/src/caches/base-cache.ts` 实现了基于 Redis 的 `connectors-well-known` 缓存：

```typescript
// 缓存键格式: `${tenantId}:connectors-well-known:#`
// 缓存值: ConnectorWellKnown[] → { id, metadata, connectorId }
// 过期时间: 默认 30 分钟
```

#### 3.1.2 缓存失效触发点（INSERT/UPDATE/DELETE）

`packages/core/src/queries/connector.ts:76-84` 中所有写操作都通过 `wellKnownCache.mutate()` 包装：

```typescript
const insertConnector = wellKnownCache.mutate(
  buildInsertIntoWithPool(pool)(Connectors, { returning: true }),
  ['connectors-well-known']  // 执行成功后失效此缓存键
);

const updateConnector = wellKnownCache.mutate(
  buildUpdateWhereWithPool(pool)(Connectors, true),
  ['connectors-well-known']
);

const deleteConnectorById = wellKnownCache.mutate(
  async (id: string) => { /* ... */ },
  ['connectors-well-known']
);

const deleteConnectorByIds = wellKnownCache.mutate(
  async (ids: string[]) => { /* ... */ },
  ['connectors-well-known']
);
```

**失效机制**：
```typescript
mutate(run, ...types) {
  const value = await run(...args);
  // 注意：使用 void + Promise.all，不等待删除完成
  void Promise.all(
    types.map(async ([type, cacheKey]) =>
      trySafe(kvCache.delete(type, cacheKey?.(...args) ?? BaseCache.defaultKey))
    )
  );
  return value;  // 立即返回，不等待缓存删除
}
```

#### 3.1.3 失效路径总结

| 操作 | API 路由 | 缓存失效 |
|-----|---------|---------|
| 创建连接器 | `POST /connectors` | `insertConnector` → 缓存删除 |
| 更新连接器 | `PATCH /connectors/:id` | `updateConnector` → 缓存删除 |
| 删除连接器 | `DELETE /connectors/:id` | `deleteConnectorById` → 缓存删除 |
| 批量删除 | 内部调用 | `deleteConnectorByIds` → 缓存删除 |

**重要特性**：
- **异步失效**：缓存删除是 fire-and-forget 模式（`void Promise.all`）
- **失败静默**：`trySafe` 包裹缓存删除，Redis 失败不影响主流程
- **双重读取**：`getLogtoConnectors()` 不使用缓存（每次读全表），`findAllConnectorsWellKnown()` 使用缓存

### 3.2 普通连接器配置更新约束

`packages/core/src/routes/connector/index.ts:276-369` 中的 PATCH 接口：

#### 3.2.1 `isStandard` 元数据限制

**创建时约束**（`POST /connectors`）：

```typescript
if (connectorFactory.type === ConnectorType.Social) {
  // 标准连接器必须指定 target
  assertThat(
    connectorFactory.metadata.isStandard !== true || Boolean(metadata?.target),
    'connector.should_specify_target'
  );
  
  // 非标准连接器不可覆盖 metadata
  assertThat(
    connectorFactory.metadata.isStandard === true || metadata === undefined,
    'connector.cannot_overwrite_metadata_for_non_standard_connector'
  );
  
  // 非标准连接器仅允许一个实例
  const { count } = await countConnectorByConnectorId(connectorId);
  assertThat(
    count === 0 || connectorFactory.metadata.isStandard === true,
    new RequestError({ code: 'connector.multiple_instances_not_supported', status: 422 })
  );
}
```

**更新时约束**（`PATCH /connectors/:id`）：

```typescript
// 标准连接器不可修改 target
assertThat(
  originalMetadata.isStandard !== true ||
    !metadata ||
    metadata.target === originalMetadata.target,
  'connector.can_not_modify_target'
);

// 非标准连接器不可修改 metadata
assertThat(
  originalMetadata.isStandard === true || !metadata,
  'connector.cannot_overwrite_metadata_for_non_standard_connector'
);
```

#### 3.2.2 Target-Platform 唯一性约束

`packages/core/src/utils/connectors/platform.ts:6-26`：

```typescript
export const checkSocialConnectorTargetAndPlatformUniqueness = (connectors: LogtoConnector[]) => {
  const targetAndPlatformSet = new Set<string>();
  
  for (const { metadata: { target, platform } } of connectors.filter(type === Social)) {
    if (platform === null) continue;  // platform 为 null 时不检查
    
    const element = JSON.stringify([target, platform]);
    assertThat(!targetAndPlatformSet.has(element), 
      'connector.multiple_target_with_same_platform');
    targetAndPlatformSet.add(element);
  }
};
```

**检查触发点**：`GET /connectors` 接口每次调用前执行检查。

**可见性影响**：
- 违反唯一性约束的组合不会被创建/更新（API 422 错误）
- `platform = null` 的连接器跳过检查，允许多个同 target 实例
- 该检查是"防御性检查"，不保证数据库层面的绝对唯一性

#### 3.2.3 Token Storage 门禁机制

**启用条件**（双重门禁）：

```typescript
if (enableTokenStorage) {
  // 门禁1：环境配置 - 必须配置 secretVaultKek
  assertThat(
    EnvSet.values.secretVaultKek,
    new RequestError({ code: 'request.feature_not_supported', status: 422 })
  );
  
  // 门禁2：连接器能力 - 必须是社交连接器且声明支持
  assertThat(
    connectorFactory.type === ConnectorType.Social &&
      connectorFactory.metadata.isTokenStorageSupported,
    new RequestError({ code: 'connector.token_storage_not_supported', status: 422 })
  );
}
```

**关闭副作用**：

```typescript
if (type === ConnectorType.Social && 
    originalMetadata.isTokenStorageSupported &&
    enableTokenStorage === false) {
  // 关闭Token存储时级联清理已存储的所有Token
  await tenant.queries.secrets.deleteTokenSetSecretsBySocialConnectorId(id);
}
```

**故障边界影响**：
- Token Storage 门禁失败不影响连接器基本功能（仅该功能不可用）
- 关闭 Token Storage 是破坏性操作（数据永久删除）
- `isTokenStorageSupported = false` 的连接器永远无法启用该功能

#### 3.2.4 "通知"机制 —— 无状态设计

**关键观察**：系统采用**拉模式**而非**推模式**：

1. 配置更新仅持久化到数据库 (`updateConnector`)
2. `connectors-well-known` 缓存被异步删除失效
3. 每次调用 `getLogtoConnectors()` 都会重新从DB读取并实例化
4. 配置变更对后续请求自然生效，无需显式 reload

### 3.3 SSO 连接器可用性检查

`packages/core/src/libraries/sso-connector.ts:51-61` 中的 `getAvailableSsoConnectors()`：

```typescript
return connectors.filter(({ providerName, config, domains }) => {
  const factory = ssoConnectorFactories[providerName];
  const hasValidConfig = factory.configGuard.safeParse(config).success;
  const hasValidDomains = domains.length > 0;
  return hasValidConfig && hasValidDomains;
});
```

- 每次调用时**即时验证**配置有效性
- 配置无效的连接器被过滤，不对外暴露
- 实现了"配置变更自动感知"

### 3.4 连接器工厂缓存机制

`packages/cli/src/connector/factories.ts:12-60` 实现了工厂级缓存：

```typescript
let cachedConnectorFactories: Array<ConnectorFactory> | undefined;

export const loadConnectorFactories = async (...) => {
  if (cachedConnectorFactories) {
    return cachedConnectorFactories;  // 直接返回缓存
  }
  // ... 加载逻辑
  cachedConnectorFactories = connectorFactories.filter(Boolean);
  return cachedConnectorFactories;
};
```

**缓存特性**：
- 仅缓存连接器**工厂**（代码元数据），不缓存配置实例
- 启动后不再刷新，工厂代码变更需重启服务
- 连接器**配置**变更不受此缓存影响，每次从DB读取

---

## 4. 加载失败应对策略

### 4.1 启动期加载失败

#### 4.1.1 单个连接器加载失败 —— 优雅降级

`packages/cli/src/connector/factories.ts:22-49`：

```typescript
const connectorFactories = await Promise.all(
  connectorPackages.map(async ({ path: packagePath, name }) => {
    try {
      const createConnector = await loadConnector(packagePath, ignoreVersionMismatch);
      const rawConnector = await createConnector({ getConfig: notImplemented });
      validateConnectorModule(rawConnector);
      return { metadata, type, configGuard, createConnector, path: packagePath };
    } catch (error: unknown) {
      if (error instanceof Error) {
        consoleLog.error(
          `[load-connector] skip ${chalk.bold(name)} due to error: ${error.message}`
        );
        return;  // 返回 undefined，后续被过滤
      }
      throw error;  // 非Error类型异常继续抛出
    }
  })
);
```

**策略**：
- 单个连接器加载失败 → 记录错误日志 → 返回 undefined
- 最终结果过滤掉 undefined，不影响其他连接器
- 服务正常启动，仅缺失失败的连接器

#### 4.1.2 致命失败场景

以下错误会导致启动终止：
1. 扫描 `connectors` 目录时发生IO异常
2. 发现重复的连接器工厂ID (`checkDuplicateConnectorFactoriesId`)
3. 非 `Error` 类型的异常被抛出

### 4.2 运行时静默失败点汇总

#### 4.2.1 核心静默失败点

| 位置 | 代码 | 失败影响 | 是否有日志 |
|-----|------|---------|-----------|
| `packages/core/src/libraries/connector.ts:112` | `} catch {}` | 实例化失败的连接器从结果中过滤 | ❌ 空 catch，无日志 |
| `packages/core/src/libraries/connector.ts:54-56` | `if (!connectorFactory) { return; }` | 工厂不匹配的 DB 记录被静默跳过 | ❌ 无日志 |
| `packages/core/src/libraries/connector.ts:58-66` | `return trySafe(async () => { ... })` | getLogtoConnectorsWellKnown 中构建失败静默跳过 | ❌ trySafe 吞异常 |
| `packages/core/src/caches/base-cache.ts:52` | `return trySafe(async () => { ... })` | Redis 读取/解析失败返回 undefined | ❌ trySafe 吞异常 |
| `packages/core/src/caches/base-cache.ts:100` | `trySafe(kvCache.delete(...))` | 缓存删除失败不影响主流程 | ❌ trySafe 吞异常 |

#### 4.2.2 `getLogtoConnectors` 静默失败链

```typescript
// packages/core/src/libraries/connector.ts:77-113
const logtoConnectors = await Promise.all(
  databaseConnectors.map(async (databaseConnector) => {
    const connectorFactory = connectorFactories.find(...);
    
    if (!connectorFactory) {
      return;  // 静默失败点1: 工厂不存在
    }
    
    try {
      const { rawConnector, rawMetadata } = await buildRawConnector(...);
      // ... 构造连接器
      return { ...connector, validateConfig, dbEntry };
    } catch {  // 静默失败点2: 空 catch，吞掉所有异常
      // 包括但不限于：
      // - 模块导入失败
      // - validateConnectorModule 失败
      // - 元数据解析失败
      // - 配置初始化失败
    }
  })
);

return logtoConnectors.filter(Boolean);  // 过滤所有 undefined
```

**故障边界影响**：
- 单个连接器配置错误可能导致该连接器"消失"
- 无任何告警或日志，排查困难
- 数据库中存在的连接器可能不在 API 返回结果中
- **故障隔离**：单个连接器失败不影响其他连接器

#### 4.2.3 `trySafe` 静默失败

`@silverhand/essentials` 的 `trySafe` 函数被广泛用于吞异常：

```typescript
// WellKnownCache.get() - Redis 失败返回 undefined
return trySafe(async () => {
  const data = await this.cacheStore.get(this.cacheKey(type, key));
  return this.getValueGuard(type).parse(JSON.parse(data ?? ''));
});

// WellKnownCache.mutate() - 缓存删除失败不影响主流程
void Promise.all(
  types.map(async ([type, cacheKey]) =>
    trySafe(kvCache.delete(type, cacheKey?.(...args) ?? BaseCache.defaultKey))
  )
);
```

### 4.3 连接器调用错误处理

#### 4.3.1 错误码体系 (`ConnectorErrorCodes`)

定义于 `packages/toolkit/connector-kit/src/types/error.ts`：

| 错误码 | HTTP状态 | 场景 |
|-------|---------|------|
| `InvalidMetadata` | 400 | 元数据缺失或非法 |
| `InvalidConfigGuard` | 400 | 缺少配置验证Schema |
| `InvalidConfig` | 400 | 配置验证失败 |
| `InvalidCertificate` | 400 | 证书无效 (SAML) |
| `InvalidResponse` | 400 | 第三方响应格式错误 |
| `SocialAuthCodeInvalid` | 401 | 授权码无效 |
| `SocialAccessTokenInvalid` | 401 | Access Token无效 |
| `AuthorizationFailed` | 401 | 授权失败 |
| `RateLimitExceeded` | 429 | 触发第三方限流 |
| `NotImplemented` | 501 | 功能未实现 |

#### 4.3.2 错误转换中间件

`packages/core/src/middleware/koa-connector-error-handler.ts` 将 `ConnectorError` 转换为 `RequestError`：

```typescript
switch (code) {
  case ConnectorErrorCodes.InvalidMetadata:
  case ConnectorErrorCodes.InvalidConfig:
  case ConnectorErrorCodes.InvalidResponse:
    throw new RequestError({ code: `connector.${code}`, status: 400 }, data);
  case ConnectorErrorCodes.SocialAuthCodeInvalid:
  case ConnectorErrorCodes.AuthorizationFailed:
    throw new RequestError({ code: `connector.${code}`, status: 401 }, data);
  case ConnectorErrorCodes.RateLimitExceeded:
    throw new RequestError({ code: `connector.${code}`, status: 429 }, data);
}
```

#### 4.3.3 SSO 连接器错误扩展

`packages/core/src/sso/types/error.ts` 定义了 `SsoConnectorError`，继承自 `ConnectorError` 并增加了更细粒度的配置错误码：

```typescript
enum SsoConnectorConfigErrorCodes {
  InvalidSamlXmlMetadata = 'invalid_saml_xml_metadata',
  InvalidConfigResponse = 'invalid_config_response',
  FailToFetchConfig = 'fail_to_fetch_config',
  InvalidConnectorConfig = 'invalid_connector_config',
}
```

### 4.4 配置验证失败处理

`validateConfig` 函数在三个关键节点被调用：

1. **创建连接器时** (`POST /connectors`)：提前验证配置有效性
2. **更新连接器时** (`PATCH /connectors/:id`)：确保更新后配置合法
3. **实例化后注入**：每个连接器实例自带 `validateConfig` 方法，供运行时调用

验证失败时抛出 `ConnectorErrorCodes.InvalidConfig` 错误。

---

## 5. 架构总结与设计权衡

### 5.1 两种连接器体系对比

| 维度 | 普通连接器 (Social/Email/SMS) | SSO 连接器 |
|-----|-----------------------------|------------|
| 加载方式 | 文件系统动态扫描 | 代码静态注册 |
| 包名规则 | `connector-*` 或 `@scope/connector-*` | 无（代码内注册） |
| 扩展性 | 支持第三方连接器包 | 仅支持内置实现 |
| 配置位置 | `connectors` 表 | `sso_connectors` 表 |
| 元数据定义 | `ConnectorMetadata` | `SingleSignOnFactory` |
| 实例化时机 | 每次调用 `getLogtoConnectors` | 使用时 `new` 构造 |
| 缓存策略 | 工厂缓存 + 无状态实例 + WellKnown缓存 | 无缓存 |
| 配置失败 | 静默过滤，连接器消失 | `getAvailableSsoConnectors` 过滤 |

### 5.2 关键设计决策

1. **无状态实例化**：每次请求重新构建连接器实例，配置变更无需额外通知机制
2. **优雅降级**：单个连接器失败不影响整体服务可用性
3. **Zod 驱动验证**：从元数据到配置全链路类型安全
4. **工厂与实例分离**：工厂代码启动后固定，配置数据运行时可变
5. **异步缓存失效**：Connectors-Well-Known 缓存删除是 fire-and-forget 模式，Redis 故障不影响核心流程

### 5.3 标准连接器 vs 非标准连接器约束对比

| 特性 | 标准连接器 (isStandard=true) | 非标准连接器 |
|-----|-----------------------------|-------------|
| 允许多实例 | ✅ 允许 | ❌ 仅允许一个实例 |
| 可覆盖 metadata | ✅ 允许（target/name/logo/logoDark） | ❌ 禁止 |
| 创建时需指定 target | ✅ 必须 | ❌ 使用工厂默认 target |
| 可修改 target | ❌ 禁止 | - |

### 5.4 潜在改进点

1. **运行时实例化失败无日志**：`getLogtoConnectors` 中的空 catch 可能导致问题难以排查
2. **缺少配置变更审计**：配置更新仅落库，无历史记录或事件通知
3. **工厂缓存无法刷新**：新增连接器包需重启服务才能加载
4. **无健康检查机制**：连接器配置失效后无自动告警
5. **数据库无唯一约束**：target-platform 唯一性仅在 API 层检查，数据库层可绕过
6. **静默失败点过多**：多处 trySafe 和空 catch 可能隐藏真实问题

---

## 6. 关键代码位置索引

| 功能模块 | 文件路径 |
|---------|---------|
| 元数据类型定义 | `packages/toolkit/connector-kit/src/types/metadata.ts` |
| 基础连接器类型 | `packages/toolkit/connector-kit/src/types/foundation.ts` |
| 社交连接器类型 | `packages/toolkit/connector-kit/src/types/social.ts` |
| 连接器错误定义 | `packages/toolkit/connector-kit/src/types/error.ts` |
| validateConnectorModule | `packages/cli/src/connector/utils.ts:22` |
| SSO连接器工厂 | `packages/core/src/sso/index.ts` |
| SSO连接器类型 | `packages/core/src/sso/types/index.ts` |
| 工厂加载逻辑 | `packages/cli/src/connector/factories.ts` |
| 模块加载与版本检查 | `packages/cli/src/connector/loader.ts` |
| 包名准入规则 | `packages/cli/src/utils.ts:239-272` |
| 连接器库核心逻辑 | `packages/core/src/libraries/connector.ts` |
| SSO连接器库 | `packages/core/src/libraries/sso-connector.ts` |
| 连接器API路由 | `packages/core/src/routes/connector/index.ts` |
| Connectors-Well-Known缓存 | `packages/core/src/queries/connector.ts:27-37` |
| 缓存失效包装器 | `packages/core/src/caches/base-cache.ts:85-108` |
| Target-Platform唯一性检查 | `packages/core/src/utils/connectors/platform.ts` |
| 启动加载入口 | `packages/core/src/main.ts` |
| 错误处理中间件 | `packages/core/src/middleware/koa-connector-error-handler.ts` |
